{
  "empty_program": "Your workspace is empty! Drag a 'move forward' card from the Movement toolbox onto the canvas to get started.",
  "unbalanced_block": "Looks like you started a block (like repeat or if) but never added its matching end card. Every open card needs a closing partner - like end repeat or end if - right after everything you want inside it.",
  "infinite_loop": "Your program got stuck looping forever! If you used a while block, check that its condition can actually become true - like reaching the goal - so the loop knows when to stop."
}
