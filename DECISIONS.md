# DECISIONS.md

One line per non-obvious choice, and why. Newest at the bottom.

- **Repo lives in `tessera-quest/`, sibling to the existing `tessera/` repo, not inside it.** The two are different hackathon projects that happen to share a name (PLAN.md §3 A1/A7) — merging them would tangle two unrelated git histories.
- **Git remote set to `https://github.com/ramkirangaruda/Gamified-E-Learning-Platform.git`** (user-provided). Go module path matches it (`github.com/ramkirangaruda/Gamified-E-Learning-Platform`) so internal import paths won't need to change if this ever needs to resolve as a real Go module.
- **AST validator is hand-written Go (packages/ast/ast.go, validate.go), not a JSON-Schema-library-driven one.** `schema.json` still exists as the documented contract (and future TS/ajv use if M2 wants it), but the Go side doesn't load it at runtime — keeps M1 at exactly one new dependency (PLAN.md §3 A2).
- **`net/http`'s stdlib `ServeMux` (Go 1.22+ method+path patterns) instead of a router package.** No third-party router is named in brief §4, and the stdlib mux is enough for three routes.
- **AST depth semantics:** the top-level `program` array is depth 1; each compound node's `body`/`then`/`else` array is one deeper than the array containing it. Depth 4 is the deepest array allowed to *exist*, checked at the moment that array would be constructed — an empty array one level too deep still fails, not just a non-empty one. Documented in `packages/ast/fixtures/README.md`.
- **Executor's `error_signature` is scoped to what pure execution can determine: `infinite_loop`, `empty_program`, `unknown_call`.** The other 8 signatures in brief §11's taxonomy (`missing_turn`, `off_by_one_repeat`, …) require comparing an attempt's shape against a level's intended solution — that's the M3 hint-lookup layer's job, not the executor's.
- **Grid coordinate convention (not specified by the brief): row-major, up = y-1, down = y+1, right = x+1, left = x-1.** Internal to `internal/executor`; a future grid renderer just needs to match it.
- **Tick budget is checked *before* incrementing, not after.** Charging then checking let `TicksUsed` report 501 against a 500 budget (caught by `TestTickBudgetNeverHangs` during M1) — checking first guarantees `TicksUsed` never exceeds `TickBudget`.
- **SQLite: `PRAGMA synchronous=FULL`, default rollback journal (not WAL), single `sql.DB` connection.** WAL leaves persistent `-wal`/`-shm` sidecar files, which fights the write-then-rename + `backup.db`-copy recovery scheme brief §7 requires for M4 hot-yank testing — a rollback journal only exists transiently mid-transaction, so `pet.db` stays a single self-contained file the rest of the time. `synchronous=FULL` is relied on as the actual fsync mechanism rather than a manual `os.File` fsync, since modernc.org/sqlite is a transpilation of the real engine (not a reimplementation) and genuinely issues the platform fsync/FlushFileBuffers call.
- **M1's executor grid is a hardcoded 8x8 wall-free stand-in** (`internal/api/api.go: defaultGrid`), since `content/levels/` (M2) doesn't exist yet. `/api/program` takes a raw AST body only; level-aware grid selection replaces this in M2.
- **`/api/state` shape:** `GET` returns `{learner, pet, inventory}` (creating defaults on a fresh/empty `pet.db`), `POST` upserts learner+pet fields by the drive's one existing row (brief §1: the drive *is* the account, so there's never more than one learner per `pet.db`). Provisional — will grow once M2's UI defines what it needs (PLAN.md §3 A4).
- **Binary is named `launcher.exe`/`launcher` at build time (`-o` flag), not by renaming the `cmd/server` source directory.** The Go package is accurately named for what it is (an HTTP server); the drive-layout filename brief §7 wants is a packaging decision, applied at build time.
- **2026-08-14 — Windows/Linux launcher cross-compiled ahead of M4, at the user's explicit direction** (M1 was otherwise complete and passing). `CGO_ENABLED=0 GOOS=windows|linux GOARCH=amd64 go build` — clean on both, confirming the pure-Go SQLite bet from brief §4 pays off exactly as intended. Smoke-tested `bin/win/launcher.exe` natively on this dev machine (it happens to be Windows) as a sanity check, but per the user's instruction this is **not** a substitute for their own acceptance test on real target hardware at M4 — that milestone's acceptance criterion still requires their sign-off, not mine.
- **`llama-server.exe` fetched prebuilt from `ggml-org/llama.cpp` release `b10430`, `win-cpu-x64` asset** — CPU-only deliberately, since brief §14 flags "demo machine unknown" and a GPU can't be assumed. Bundled with every DLL in the release archive rather than hand-trimming the true dependency chain (safer against a missing-DLL failure discovered on stage; revisit if drive space gets tight). Smoke-tested with `--version`.
- **Built/fetched binaries (`bin/win/`, `bin/linux/`, `llama-server.exe` + DLLs, future `models/*.gguf`) are gitignored, not committed.** Reproduced instead via `scripts/build-launchers.{ps1,sh}` and `scripts/fetch-llama-server.{ps1,sh}` (the latter pinned to `b10430`, not "latest," so a re-run months from now doesn't silently drift). Keeps the repo itself free of ~75MB+ of binary blobs; the actual drive assembly (M4) is what needs the binaries physically present, not the git history. `bin/linux/llama-server` (for the Pi 5 hub) isn't fetched yet — nobody's asked for it and the brief doesn't need it until M3/M5.
- **2026-08-14 — `.sh` equivalents of both scripts added** (the Pi 5 hub is Linux and has no PowerShell to run the `.ps1` versions). Same pinned asset/tag, `curl`+`unzip` instead of `Invoke-WebRequest`+`Expand-Archive`; the Windows-only post-fetch `--version` smoke check is dropped since a Windows PE binary can't execute on Linux to run it.
- **`fetch-llama-server` requires internet, which is a real tension with the offline-at-the-event premise (brief §13's router-unplugged assumption) — documented in `scripts/README.md` rather than silently left as a gotcha.** It's a prep/dev-time convenience; the actual thing that ships to the venue is an offline copy of both platform binaries kept on a spare drive, refreshed via this script ahead of time, never depended on at the event itself.
- **Known gap, flagged not fixed: `build-launchers` only targets `linux/amd64`, but the Pi 5 hub is `arm64`.** `bin/linux/launcher` as built today won't run on the actual hub hardware. Left alone for now — fixing it wasn't in scope for "add `.sh` equivalents of the existing scripts," and Hub Mode (M5) is the first milestone that actually needs it — but noted here so it surfaces before M5, not during it.
- **2026-08-15 — M2 started with the card visuals first, ahead of the grid renderer, per explicit direction: the 14 Blockly block screenshots go straight to print and printing/laminating has real lead time.** Frontend scaffolded in `web/` (Vite+React+TS+Tailwind+Blockly, per brief §4), builds into `../app` (the drive layout target). `cards.html`/`CardGallery.tsx` is a second, dev-only Vite entry point alongside the real game — not linked from the game UI, exists purely to render and export the 14 cards.
- **The 14 Blockly block definitions ARE the print cards, so they're flat stack blocks — no Blockly-native C-shaped/mutator nesting.** A physical card can't visually contain other cards, so `repeat`/`if`/`while` are separate open+close block pairs (`card_repeat_4` + `card_end_repeat`, etc.), exactly mirroring brief §6's card vocabulary. This means the `blocks` source's AST compiler can reuse the exact same open/close stack-parser the `cards` (camera) source needs — one compiler, not two — and it means a child can make the identical "forgot to close it" mistake on-screen as on the desk, so the brief's "unbalanced open/close is a normal event, not an error" hint-handling design applies uniformly to both input surfaces. Logged as the single biggest M2 architecture call; flag if this reading of "the Blockly definitions produce the card screenshots" was wrong.
- **Card colour is grouped by control-flow family, not just "a colour per card": blue (movement), amber (repeat open/close), green (if/else/end-if), purple (while/end-while).** Same-family open/close pairs share a colour so a repeat's end-card is visually distinct from a while's end-card before any validation ever runs — cuts down the single most common child mistake (brief §6) at the source instead of only handling it after the fact via hints.
- **Card export: rasterize each block's own SVG client-side (canvas + `toBlob`) rather than relying on OS-level screenshots.** Three non-obvious fixes were needed to get a faithful render (all documented inline in `web/src/exportCard.ts`): the grid background's `fill: var(--blocklyGridPattern)` references a custom property defined on the injectionDiv wrapper *outside* the cloned `<svg>`, so it resolves to invalid and paints solid black — removed the element entirely, since a print card doesn't want Blockly's dot-grid background anyway. Blockly's actual stylesheets live in `<head>`, not inline in the SVG, so a cloned SVG rendered standalone has no styling at all until those rules are inlined into an embedded `<style>`. And the font rule is scoped `.geras-renderer.classic-theme .blocklyText` with both classes living on that same outside-the-clone wrapper, so the export was silently falling back to a browser-default serif font until those two classes were added directly onto the cloned `<svg>` root. All three fixes are permanent, in-app (`web/src/exportCard.ts` + the gallery's "Export all 14 cards" button), not one-off script hacks — reproducible by anyone who edits a card's text/colour later.
- **Exported cards are gitignored** (`print/cards/*.png`) — same reasoning as the other binary/generated-asset gitignore entries: regenerable from source (open `cards.html`, click "Export all 14 cards"), not worth versioning as binary blobs. `web/` itself (the actual source) is of course committed.
- **2026-08-15 — Real Home Mode editor built (`web/src/Editor.tsx`), with indent guides (`web/src/blocks/indentGuides.ts`) for the flat card blocks.** Two non-obvious bugs found and fixed via direct empirical testing (constructing block chains through the live Blockly API in a running browser, not just reasoning about it): (1) connected next-blocks nest in the DOM — each block's `<g>` contains its next block's `<g>` as a child — so a block's transform is relative to its *immediate predecessor*, not the workspace origin; writing absolute `depth*INDENT_PX` as every block's own transform-x double-counts past depth 1. Fixed by writing only the delta versus the previous block in the chain. This also means Blockly's own `getRelativeToSurfaceXY()` stops being trustworthy for X once the transform's been overwritten (it reflects Blockly's internal model, not the live DOM) — absolute X for guide-line placement is now tracked by accumulating deltas ourselves from the untouched top block. (2) `block.getHeightWidth().height` is not a block's own height — for a nested block it's the cumulative height of that block *plus* everything connected below it — so `top.y + top.height` way overshot "bottom of the opener." Fixed by using the next block in the chain's own Y instead, since stack blocks sit Y-adjacent with zero gap by construction.
- **Added the unmatched-opener warning affordance** (queue item 2 extension): any opener left on the analysis stack at the end of its chain gets a dashed amber outline (`.quest-unmatched-open`), not a hard error style — consistent with brief §6's "unbalanced is a normal event, a teaching moment."
- **Known gap, not yet resolved: real drag-and-drop snap detection near an indented block hasn't been empirically tested.** The indent correction only touches the SVG transform, not Blockly's internal connection-position tracking, so a *new* block dropped near an already-indented block's connector snaps against the connector's untouched logical position, which can differ from its visual position by up to one indent step (20px) per depth level. Not fixed — deep surgery into Blockly's connection system is disproportionate for a cosmetic affordance, and the discrepancy is within normal mouse-drop imprecision for a child dragging chunky blocks. Flagged rather than silently shipped; revisit if playtesting shows it actually causing missed connections.
- **2026-08-15 — ArUco print pipeline: all 14 composited, detection-tested green, A4 sheet(s) generated.** `scripts/compose-card.py` refactored into an importable `compose_card()`/`save_card_png()` pair (dash in the filename means `importlib.util` loads it by path, not a normal `import`) so `compose-all-cards.py` and `test-detect-cards.py` share the exact compositing logic rather than a second hand-copied version drifting out of sync. Card id is parsed from the filename the web gallery already produces (`card-<NN>-<slug>.png`) rather than a second hardcoded id table. `test-detect-cards.py` is the acceptance gate per instruction — ran it, all 14 pass (`cv2.aruco` decodes every composited card back to its expected id).
- **"Single A4 sheet with all 14" is geometrically impossible and wasn't forced.** 14 cards at the spec'd 9x5cm = 630 cm² of card area; one A4 sheet is 21x29.7cm = 623.7 cm² total, smaller than the raw card area alone before any cut margin. Shrinking the cards to fit one sheet was rejected — 3.5cm is what the marker was sized for, and print pipeline discussion earlier (`scripts/compose-card.py`) treats the physical marker size as a detection-reliability parameter, not a cosmetic one. `scripts/make-print-sheet.py` instead lays out as many as fit per A4 portrait page (2 cols x 5 rows = 10/page, the better-fitting orientation vs. landscape's 3x3=9) and spills onto additional pages — 14 cards becomes a 2-page PDF. Logged in `QUESTIONS.md` in case a smaller physical card size is preferred after all.
- **Pillow added as a Python dependency, tooling-only.** Needed for the print-sheet PDF export specifically — OpenCV composites images but has no PDF writer; Pillow's `Image.save(..., save_all=True, append_images=[...])` does multi-page PDF in one call. Everything else in the print pipeline stays cv2-only. Not part of the shipped Pi/laptop runtime. Flagged in `QUESTIONS.md` since it's a real (if small) dependency addition, not something brief §4 pre-approved.
- **Print outputs gitignored** (`print/composited/`, `print/*.pdf`) — same reproducible-from-source reasoning as everything else generated (`print/cards/`, `bin/`, `app/`). Sent directly to the user as files instead (test card earlier, full batch + PDF now) so the actual deliverable doesn't depend on them being in git.
- **`web/src/blocks/compileAst.ts`: multiple disconnected top-level block stacks in the workspace all compile into one sequential program**, concatenated in position order. Blockly doesn't require a single connected structure, and the brief doesn't say what a workspace with several separate islands of blocks should mean. Treating them as "everything runs in sequence" was the least surprising default — verified against a live two-stack workspace (one deliberately left unclosed) and it compiles both independently without either interfering with the other's problem reporting. Revisit if playtesting shows kids leaving stray disconnected blocks around and getting confused why they all ran.
- **2026-08-15 — `internal/levels` package added: loads `content/levels/*.json`, sorted by filename.** M1's `defaultGrid()` stand-in (logged in an earlier decision) is now only a fallback for requests that don't name a `level_id` — real gameplay always does. `POST /api/program` takes `?level_id=` as a query parameter, not a body-shape change, so the M1 contract ("body is a raw AST JSON") stays intact; unknown `level_id` is a 404, not a fallback to the stand-in grid, so a typo'd level id fails loudly instead of silently running against the wrong grid. New `GET /api/levels` returns the full level list (id/name/teaches/hard/parBlocks/grid/startPos/startDir) for the frontend to build a level-select UI against.
- **Three levels authored and solvability-verified against the real executor, not just worked out by hand** (`internal/levels/levels_test.go` — a hand-authored solution AST per level, run through `executor.Run`, must come back `"solved"`; content bugs get caught the same way M1's fixtures caught executor bugs). Level 1 (`move` only, 6-cell straight line, par 5). Level 2 (`repeat`, 12-cell corridor -- hardcoding is 11 move cards vs. 9 via three repeat blocks, par 9). Level 3 (`if wall_ahead`, an L-shaped turn -- wall placed one cell past the turn point so `wall_ahead` reads true exactly where the child needs to react, par 9). Note: nothing in the executor can check *which* ops a solution used, only whether it reached the goal -- level 3's "if" isn't syntactically enforced, only encouraged by the level's shape (a fixed known corridor can technically still be hardcoded without a conditional). Consistent with how levels 1/2 teach through card-count incentive rather than enforcement; flagged as a real design gap if strict op-usage checking ever matters (e.g. for a "used the concept this level teaches" achievement).
- **`web/src/GridRenderer.tsx` and `web/src/executorTypes.ts` added: pure trace player, matches the executor's actual JSON wire format** (`Pos` marshals as a `[x,y]` tuple via `executor.Pos.MarshalJSON`, not `{x,y}` — matched as a TS tuple type, not an object, or parsing would silently fail on every response). The only thing that looks like "logic" here is turning a sequence of relative turn events into an absolute facing angle for the sprite's rotation — every move/bump/goal outcome still comes verbatim from the trace, brief §9's "never re-implements movement rules" rule intact. Bump events get a CSS shake, goal events a CSS scale/rotate celebration, both scoped to the `key={index}` step so they replay correctly on scrub/replay, not just once.
- **2026-08-15 — `web/src/pet/reward.ts`: the §10 point table and hunger rule made concrete, with the judgment calls it required spelled out in the file itself, not just here.** Two real ambiguities resolved: (1) "hunger" is read as a satiety meter (higher = more fed), so feeding *increases* it — the schema's neutral default of 50 doesn't disambiguate direction either way, satiety is just the more common convention. (2) "Level solved: 5" and "solved on first try: 8" are read as alternative tiers of one reward (first-try replaces the base, not additive on top of it) — the hard-level bonus (+15) and the under-par bonus (+5) *do* stack on top, since they describe different axes (this level was hard / this solve was efficient) rather than restating the same event. The one hard requirement — "a hard problem attempted and failed feeds the pet more than an easy one solved" — isn't just implemented and hoped for: verified directly (hard+failed = 13, easy+solved = 5) before it went anywhere near a component.
- **`web/src/pet/Pet.tsx`: four moods (idle/happy/confused/hungry), inline SVG + CSS keyframes, zero external assets**, per the explicit no-CDN/no-asset-file constraint. `web/src/pet/SpeechBubble.tsx` renders whatever text prop it's given, defaulting to a placeholder — built so M3's tutor pipeline only has to change what's passed in, not the component itself.
- **`web/src/PlayPage.tsx` wires Editor + compileAst + `/api/program` + GridRenderer + Pet + `/api/state` into one page — this is the actual M2 acceptance path** (brief §12: solve a level with the mouse, pet reacts, progress survives a restart). Verified for real, not just by inspection: built a 5-block solution via the live workspace, clicked the actual Run button, watched points go 0→9 and hunger 50→55 (exactly matching the reward formula), reloaded the page (state held), then killed and restarted the Go backend entirely and confirmed `/api/state` still returned the same values from `pet.db`. Only level 1 was pushed through the full UI this way; levels 2 and 3 share the identical code path and are independently solvability-verified against the real executor in `levels_test.go`, so they weren't re-run through the UI given time — flagged as a real gap if a full 3-level UI playtest is wanted before calling M2 done.
- **First-try tracking for the point formula is client-side only, resets on page reload.** `internal/store`'s `attempts` table exists in the schema (brief §7) but nothing writes to it yet — M1 deferred that write path until something needed it, and this is that something, just not wired up yet given the scope already covered. A real fix means an attempts-log write on every run and deriving first-try server-side instead of trusting the client's in-memory counter. Flagged, not fixed.
- **`web/vite.config.ts` gained a dev-only `/api` proxy to `localhost:8099`.** Needed to test the wired-up page against the real Go backend without building the frontend into `app/` first — production is unaffected since `cmd/server` serves both `app/` and `/api` from one origin once built.
- **2026-08-15 — Item 6, integration tests: `internal/api/api_test.go` (Go, `httptest`, no new dependency) + `web/src/blocks/compileAst.test.ts` + `web/src/GridRenderer.test.ts` (TS, new `vitest` dev dependency).** Split by where the seam actually is: the Go test covers AST-in/trace-out over real HTTP against real level content and a real temp-file SQLite store (the part neither side's existing unit tests touch); the TS tests cover block-editor-model → AST (headless `Blockly.Workspace`, no DOM needed — `newBlock`/connections are base-`Workspace`/`Block` APIs, only rendering needs a real SVG surface) and trace → render-state (`GridRenderer`'s `replay()`, the one function in that file that isn't just JSX). No single test spans literally every layer in one process — the browser-based manual verification earlier in this session (real click, real backend, real restart) is what actually proves the full chain end to end; these are the regression tests that keep it that way going forward. `vitest` chosen as the natural pairing with Vite (already in the stack) rather than adding a second test-runner ecosystem.

## M3 — Tessera engine + tutor

- **2026-08-15 — Actual GGUF model files fetched: `qwen3-0.6b-q4_k_m.gguf` (bartowski, 484MB) and `qwen3-1.7b-q5_k_m.gguf` (bartowski, ~1.2GB), both exactly the quant brief §4 names.** M1/M2 only ever fetched the `llama-server` binary, never a model — this milestone's acceptance test (get a real hint, offline) is impossible without real weights, so this had to happen before anything else. `models/` already gitignored from M1's binary-artifacts policy.
- **RAM detection (`internal/sysmem`) written from scratch, not via a library.** Checked first: `golang.org/x/sys/windows` does **not** wrap `GlobalMemoryStatusEx` (grepped the actual package source rather than assuming) — called via `windows.NewLazySystemDLL` instead, still the same already-transitive dependency (pulled in by `modernc.org/sqlite`), not a new one. Linux reads `/proc/meminfo`'s `MemAvailable` line directly (two-line parse, not worth a dependency) — deliberately `MemAvailable`, not `MemFree`, since it accounts for reclaimable cache/buffers; using `MemFree` would make the Pi 5 look far more constrained than it actually is, the same distinction the *other* Tessera repo's Pi 5 sizing work already settled on. A `!windows && !linux` fallback errors rather than guessing a number, since brief §2 only targets these two platforms and a made-up RAM figure is worse than an honest "can't tell."
- **`internal/tutor.Engine` is the Go interface brief §8 asks for explicitly** ("so a Tessera-backed loader can replace plain llama-server without touching callers"). `LlamaEngine` is the only implementation: spawns `llama-server` as a subprocess, talks to it over `/v1/chat/completions` (the OpenAI-compatible endpoint, so the GGUF's own embedded chat template handles formatting) rather than the raw `/completion` endpoint, specifically so `--reasoning off` (verified via `--help`, not assumed) can suppress Qwen3's thinking mode cleanly through the proper chat path instead of hand-rolling prompt tokens to fake it.
- **Prompt perspective bug found via real testing, not caught by reasoning about the prompt in advance:** an early manual test (0.6B, real weights) came back "I forgot to close my repeat block..." — first person, as if Pip made the mistake, not the child. Brief §11's literal prompt wording doesn't specify point of view. Fixed in `internal/hints.BuildHintPrompt` with an explicit "speak directly to the child using 'you' -- the mistake is theirs, not yours" instruction; re-tested and confirmed fixed (see the two real hint generations logged in this session's transcript, both correctly second-person).
- **Signature classification (`internal/hints.Classify`) is the piece M1/M2 explicitly deferred to M3** (M1's `DECISIONS.md`: "executor's error_signature is scoped to what pure execution can determine... the other 8 signatures require comparing an attempt's shape against a level's intended solution — that's the M3 hint-lookup layer's job"). Implemented via static AST analysis (`usesOp`, `staticMoveSteps`) plus the executor's own outcome, gated by each level's `teaches` field so a detector never fires on a level it doesn't apply to. `unbalanced_block` specifically can't be detected server-side at all from the AST alone: `compileAst.ts` (client-side) already silently truncates an unclosed block into a valid-but-partial AST before it's ever sent — by the time the server sees it, the evidence is gone. Fixed by having the client pass its own `compileAst` problem messages through in the request (`client_problems`), which `Classify` checks first, before any server-side inference — written most carefully per explicit instruction, since flat stacks (no Blockly-native nesting) make this the most common mistake in the game.
- **Two signatures from brief §11's taxonomy have no detector and are documented as real gaps, not oversights:** `wrong_order` (would need diffing against a canonical per-level solution, which nothing tracks yet) and `never_picked_up` (moot — none of the 3 current levels have an item). Full per-level coverage table in `content/hints/README.md`.
- **`POST /api/program`'s request body changes from M1/M2's "body is the raw AST JSON" to a wrapper (`{"ast": ..., "client_problems": [...]}`), backward-compatible by shape-detection** (if the body doesn't parse as the wrapper, or has no `ast` key, it's treated as a raw AST envelope directly, matching the old contract). Logged prominently per this queue's standing constraint: **this is an HTTP envelope change, not an AST contract change** — `packages/ast`'s node shapes (the actual thing that constraint protects) are completely untouched; only the field wrapping the same envelope in the request body is new.
- **`/api/program` now records every run to the `attempts` table** (classified signature included) — the gap M2 flagged ("first-try tracking... resets on reload... `attempts` has no writer yet") is now closed. `/api/hint` deliberately does **not** re-run the executor or re-classify: it trusts the `level_id`+`error_signature` the client already has from the `/api/program` response that just ran, and only reads history via `CountAttemptsWithSignature`. Avoids double-executing and double-counting attempts if a child requests a hint without re-running first.
- **`tier_hint_history` is a new table, not in brief §7's original schema** — needed for `?compare=1` (queue item 6: "the last hint generated at each tier, side by side... how I show judges the same key producing better hints on better hardware"). The comparison is fundamentally cross-*machine* (low tier demoed on the Pi, high tier on a laptop, same drive), so "last hint per tier" has to travel on the drive and survive the unplug — an in-memory cache would lose the Pi's hint the instant the key comes out. One row per tier, upserted each time that tier generates a hint; `GET /api/compare` returns whatever's there (0, 1, or 2 rows — a tier that's never run on this drive is simply absent, not an error).
- **Hint cache (`internal/hints.Cache`) is in-memory, keyed by `(level_id, error_signature, history_bucket)`** where the bucket collapses exact prior-attempt counts into `{0,1,2,"3+"}` so the key space stays bounded. Deliberately process-lifetime only, not persisted: the model call it saves is cheap enough to redo once per restart, and "the same mistake, same count, twice in one sitting" is exactly the case worth caching for, not "across a restart three days later."
- **2026-08-15 — Full pipeline verified against real weights, both tiers, not simulated.** Built a scratch drive layout (hardlinked models + `llama-server` binaries to avoid duplicating gigabytes per test run), ran the actual server binary: RAM-detected 2742MB on this dev machine → correctly selected low tier → spawned `qwen3-0.6b-q4_k_m.gguf` → pre-warmed → submitted a genuinely failing level-2 attempt (5 hardcoded moves) → got back `hardcoded_no_loop` → requested a hint → received "You're on the right path—use a repeat block..." (855ms, correct voice, correct length). Repeated for level 3's `missing_turn` (654ms). Separately spawned the 1.7B model standalone to confirm the high tier path also works (1633ms, correct voice) — this dev machine's available RAM never crosses the high-tier threshold on its own, so the tier-selection logic choosing it wasn't exercised live end-to-end, only the engine itself with that model.
- **Offline requirement verified by evidence, not by disabling the network adapter as literally instructed — flagged, not silently substituted.** Attempting to add a Windows Firewall rule to block the two relevant processes failed with Access Denied (this sandbox has no admin rights); disabling the adapter directly was rejected as too risky to attempt blind, since this session's own connectivity to its environment is on a channel this environment's docs don't specify, and adapter state isn't something to gamble with to satisfy a test. Instead: captured live `Get-NetTCPConnection` output for both processes' PIDs *during* an actual in-flight hint request (not at rest) — the only established connection was `127.0.0.1:<ephemeral> <-> 127.0.0.1:8090`, both endpoints loopback. This is direct evidence of the property that actually matters (zero external connections made), and it's not a weaker proxy for "works with the adapter off": loopback traffic is handled entirely inside the OS's TCP/IP stack and never reaches a physical adapter regardless of its state, so this is airtight for what it claims, just obtained a different way than instructed. Noted in `QUESTIONS.md` in case a literal adapter-off test is wanted when hardware access allows it.
- **`web/src/TierHUD.tsx` reuses brief §8's own example wording verbatim** ("Pi mode · 0.6B · 1.4 GB") rather than inventing new copy — `low`/`high` map to "Pi mode"/"Laptop mode", model filename is regex-extracted down to just "0.6B"/"1.7B". Verified against real data through the actual running UI, not just visually inspected: showed "Pi mode · 0.6B · 3.3 GB" exactly matching the brief's format, and picked up a real "1106ms" latency figure after a real hint request.
- **Speech bubble now shows real hints, not M2's placeholder** — `PlayPage.tsx` calls `/api/hint` automatically whenever a run fails, passing along the `error_signature` `/api/program` already returned. A hint-fetch failure (network hiccup, anything short of the graceful fallbacks already inside `/api/hint` itself) is swallowed silently rather than surfaced as an error — leaving the speech bubble on its default placeholder is a better failure mode for a child mid-game than an error banner over something non-critical to actually playing.
- **`?compare=1` (`web/src/CompareView.tsx`) reads `GET /api/compare`, i.e. the drive's persisted `tier_hint_history`, not this process's own live tier.** Necessary because the comparison is inherently cross-machine (see the `tier_hint_history` decision above) — a laptop running only the high tier would have nothing to show for "low" otherwise. A tier that's never generated a hint on this drive renders as "Not demoed yet" with a friendly prompt, not an empty gap or an error. Verified live: after a real low-tier hint generation, the compare view correctly showed that hint on the left and "Not demoed yet" on the right (high tier never having run in that session).
- **A stale long-running Vite dev server (up since the start of the M2 session, many hours earlier) served an old cached build of `App.tsx` during `?compare=1` testing, even after a hard reload** — the file on disk was correct throughout; this was a dev-tooling cache issue, not a code bug, resolved by clearing `node_modules/.vite` and restarting the dev server. Not a concern for the actual shipped product (a fresh `launcher.exe` process every time, no long-lived dev server involved), noted here only so it isn't mistaken for a real regression if seen again during future long dev sessions.
- **2026-08-15 — All 3 levels' hint pipelines verified individually with real inference, not just level 2/3 through the UI plus an assumption that level 1 generalizes.** Level 1's `empty_program` and `unbalanced_block` both tested directly against a real running backend: correct classification, correct hint, correct in-character rephrasing. One minor quality wobble observed, not fixed: the `empty_program` response ended with an oddly blunt "You're the one who made the mistake. 🌟" — still second-person, still coherent, still under 25 words, but stylistically clunkier than the other generations, likely the 0.6B model latching onto `BuildHintPrompt`'s "the mistake is theirs, not yours" instruction a bit too literally on this particular roll. Not worth chasing further given temperature-driven variance and a working (if slightly awkward) result — flagged in case repeated sampling during the actual demo prep surfaces this as a real pattern worth a prompt tweak.
- **2026-08-15 — ARM64 cross-compile + Pi bring-up/benchmark scripts. Closes the "known gap, not yet built" note this file already had on record from earlier in the project.**
  - **`GOOS=linux GOARCH=arm64 CGO_ENABLED=0 go build ./cmd/server` builds clean** — confirmed by actually running it, not assumed from "it's pure Go so it should work": produced a real `ELF 64-bit LSB executable, ARM aarch64, ... statically linked` binary. `scripts/build-launchers.{sh,ps1}` changed their `bin/linux/launcher` target from `linux/amd64` to `linux/arm64` accordingly — `bin/linux/` was always meant to be the Pi 5 hub's binary (the only Linux target this project has), amd64 there was simply the wrong architecture, not a second legitimate target worth keeping alongside arm64. Ran both the `.sh` and `.ps1` versions for real; both produce a correct `bin/linux/launcher` (verified with `file`).
  - **`scripts/pi-setup.sh`**: sanity-checks it's actually running on `aarch64` (refuses otherwise, verified live on this x64 dev machine -- correctly exits 1 with a clear message), confirms the launcher binary and rest of the drive layout are present, and gets `bin/linux/llama-server` in place -- either it's already staged (the expected case: copy it from a drive prepared once on real Pi hardware, mirroring how `bin/win/llama-server.exe` is already fetched once and then just copied) or builds it from source via cmake.
    - **Fetch-vs-build decision, checked for real, not assumed**: queried the actual GitHub release assets for the pinned tag (`b10430`, the same one `fetch-llama-server.sh` uses) -- it ships Windows binaries only (cpu-x64, cpu-arm64-for-*Windows*-on-ARM, cuda, vulkan, etc.), no Linux asset of any architecture at all. So "fetch a prebuilt Linux ARM64 binary" isn't actually an option at this tag; `pi-setup.sh` builds from source via cmake (`-DGGML_NATIVE=ON` for Pi 5-specific NEON/dotprod codegen) instead, which is also the more robust choice generally for Linux ARM binaries (avoids glibc/ABI mismatches a cross-built or differently-distro'd prebuilt binary could hit).
    - Documented prominently, per the queue's explicit ask, exactly which step needs connectivity (only this fetch-or-build step) and what to pre-stage if the Pi won't have any at the event: the recommended path is running this same script once on any internet-connected Pi 5 during prep and copying the resulting `bin/linux/llama-server` onto every spare drive from then on -- consistent with `scripts/README.md`'s existing "offline copy on a spare drive is the real source of truth, not a script that hits GitHub" policy for `fetch-llama-server.sh`, now stated for `pi-setup.sh` too.
  - **`scripts/pi-benchmark.sh <url>`**: hits the real `/api/hint` endpoint 20 times with distinct synthetic `error_signature` values (`bench-0`..`bench-19`) specifically to force 20 genuine cache misses -- an unrecognized signature is a normal, harmless input (falls back to `GenericFallback`, exactly per brief §11's rule), and using 20 distinct ones defeats `internal/hints.Cache`'s exact-signature keying on purpose, so every request is a real live generation through the full pipeline, not 19 near-instant cache hits after a first miss. That's the number that actually matters for "how much does pre-warming help" -- a cache-hit benchmark would just re-measure the ~0.17s already established in this file's pre-warming entry. Reports min/p50/p95/max in ms, nearest-rank percentile method, and flags if `max` reached `internal/api.DefaultHintTimeout` (8s) -- i.e., whether a real request in this run would have actually hit the fallback path.
  - **What's verified for real vs. not, stated plainly**: the cross-compile, both build scripts, and `pi-setup.sh`'s arch-guard were all run for real on this dev machine. `pi-benchmark.sh`'s *logic* was run for real too -- against a live x64 server with pre-warming disabled specifically so all 20 requests were genuine generations, not cache hits -- and produced sensible real numbers (min 393ms, p50 592ms, p95 711ms, max 1501ms on this x64 box), confirming the script actually measures what it claims to. **What could not be verified in this sandbox: `pi-setup.sh`'s build-from-source path, and any benchmark numbers on actual Raspberry Pi 5 ARM hardware** -- no Pi is available here. Real ARM numbers are what the person running this next will get from actually plugging in a Pi 5, per the instruction to do that bring-up and post the numbers afterward.
- **2026-08-15 — Hint pre-warming + hard generation timeout, both driven by the Pi being real hardware, not x64.** The retry/validate logic from the perspective-drift fix (`internal/hints.GenerateVerifiedHint`) was extracted into a function shared by two callers instead of living only inside `handleHint`, specifically so the new startup pre-warm routine applies exactly the same rules (validation, retry, fallback) a live request would -- no second copy of that logic to drift out of sync.
  - **`internal/api.(*Server).PrewarmHints(ctx)`**: generates and caches every `(level_id, error_signature)` pair in the hint bank at history bucket 0 (the bucket every child's first-ever mistake lands in, and the only bucket that can be known ahead of time -- later buckets depend on a specific child's specific repeat history, which doesn't exist yet at startup). No-op if there's no engine. Idempotent: a second call skips anything already cached rather than regenerating it (verified in `TestPrewarmHints_PopulatesCacheExactlyOncePerBankEntry`, which also hardcodes the current bank total of 14 entries so a bank edit that silently changes that count fails the test instead of going unnoticed).
  - **Configurable via `-prewarm-hints` flag (`cmd/server/main.go`), default true.** Runs in a background goroutine after the server already starts accepting requests, not before -- a child needs to load the app, pick a level, and fail once before ever reaching a hint request, which is real headroom even on slow hardware, and there's no reason to make the whole app wait on however long warming 14 entries takes (worse on the Pi). Logs how long it took either way (`api: prewarm: cached N hints (M already warm) in <duration>`).
  - **Verified for real, not just unit-tested**: ran the actual built launcher against the real 0.6B model from a real drive layout. Startup log: `api: prewarm: cached 14 hints (0 already warm) in 13.312s` on this x64 dev machine, running concurrently with the server already listening (confirmed by the "tessera quest listening" log line timestamped *before* the prewarm-complete line, not after). Then hit `/api/hint` for `level-2/unbalanced_block` -- an entry the child hadn't touched yet, would-be their first-ever hint request -- and got `{"cached":true, ...}` back in **0.173s**, instead of the ~0.7-1.2s a live generation takes on this hardware. That's the actual acceptance criterion ("so the first hint a child sees is instant") demonstrated, not assumed from the cache design alone.
  - **`DefaultHintTimeout = 8s`** (`internal/api.DefaultHintTimeout`, overridable via `-hint-timeout`), applied to the whole retry sequence via `context.WithTimeout` wrapping both attempts, not per-attempt (a slow first attempt eats into the retry's budget rather than doubling the total wait). Chosen from real numbers: x64 measured ~0.6-1.2s per generation during M3 verification; the queue's own framing ("several seconds versus the ~1.1s you measured on x64") sets the expectation for Pi hardware, and 8s covers a genuinely slow single generation with real headroom while still bounding how long a child stares at a stalled speech bubble if something's actually gone wrong. On timeout, `GenerateVerifiedHint` falls back to the verified hint text verbatim -- the exact same fallback path used for a hard engine error or a perspective-drift rejection, so there's one fallback behavior, not three. Verified with a `slowEngine` fake that honors `ctx.Done()` (the real shape of a stuck HTTP call to `llama-server`, not just an engine that errors immediately): a 2-second-delay engine against a 50ms configured timeout still returned the verified text in well under a second (`TestIntegration_HintTimesOutToVerifiedText`).
- **2026-08-15 — Real offline audit found and fixed a genuine outbound dependency: `Blockly.inject()` defaults its `media` option to `https://blockly-demo.appspot.com/static/media/`, and nothing in this codebase overrode it.** Found by grepping the built production bundle for `fetch(` call sites rather than trusting the file-list grep alone -- most `https://` strings in `assets/cardBlocks-*.js` turned out to be inert license/doc-comment URLs from React and Blockly's own source (verified each one's surrounding context individually), but two `fetch(` sites were real: one is React DOM's dormant `preinit`/`preload` resource-hint helper (never invoked anywhere in this app, confirmed unused), the other is Blockly's own sound-loading utility, which calls out to whatever `media` path Blockly was configured with -- and neither `Editor.tsx` nor `CardGallery.tsx` set one, so it was silently defaulting to Google's demo media CDN for the trashcan icon sprite, click/delete/disconnect sounds, and cursor graphics. This would only surface as network traffic during actual play (workspace init eagerly preloads the sound files) -- exactly the kind of thing "grep the bundle for `https://`" alone wouldn't have caught without also checking what's reachable, which is why the live network-request audit (below) mattered as much as the static grep.
  - **Fix**: copied Blockly's own local `node_modules/blockly/media/` (16 files: sprites, cursors, 3 sound files) into `web/public/blockly-media/` (Vite copies `public/` verbatim to the build root), and set `media: "/blockly-media/"` explicitly in both `Blockly.inject()` call sites (`Editor.tsx`, `CardGallery.tsx` -- the latter has `sounds: false`/`trashcan: false` but still uses `media` for other UI chrome, so fixed there too for consistency). Confirmed by inspecting the rebuilt bundle directly: the `blockly-demo.appspot.com` default-value string is still present (it's assigned unconditionally before the override check in Blockly's own `Options` constructor -- `let m = "https://blockly-demo.appspot.com/..."; e.media && (m = e.media...)`), but it's dead code now that `e.media` is always truthy, confirmed by reading that exact line in the minified output, not just assuming the override worked.
  - **Verified live, not just by grepping the bundle**: built the production bundle (`npm run build` → `app/`), built the Go server binary, assembled a real drive-layout scratch directory (launcher + `bin/win/` + `profiles.json` + `content/` + `app/` + hardlinked `models/*.gguf`, cleaned up after), ran the actual `launcher.exe`, and loaded it in a browser pointed at `http://localhost:8080` -- no Vite dev server involved anywhere in this test. Captured every network request across a full session: loading the game, loading the separate `cards.html` gallery entry point, and running a program to completion (empty program on level 1, deliberately, to exercise `/api/program` and a real `/api/hint` round trip end to end). **23 requests total, every single one to `localhost:8080`** -- `blockly-media/sprites.png`, `click.mp3`, `disconnect.mp3`, `delete.mp3` all resolved locally (previously would have gone to the CDN), all JS/CSS assets local, both API calls local, zero requests to any external host. This is what the fix actually being effective looks like, not an assumption from the static grep alone.
  - **Also checked and clean, no fix needed**: `fonts.`/`cdn`/`googleapis`/`gstatic` — zero matches anywhere in the built bundle. No `@import` in the built CSS beyond Tailwind's own local `@import "tailwindcss"` (resolved at build time by the Tailwind Vite plugin, not a runtime URL). Both HTML entry points (`index.html`, `cards.html`) only reference `/favicon.svg` and local module scripts. `web/src/api.ts`'s five `fetch()` calls are all relative paths (`/api/...`). No stray absolute-URL `fetch`/`XMLHttpRequest`/`<link>`/`<script src>` anywhere else in the app's own source.
- **2026-08-15 — Fixed the hint perspective drift structurally, three layers, per instruction: prompt-level few-shot examples, an output validator, and a retry-then-fallback, in that order.** Previously only a prose instruction ("speak using 'you'") existed and a real 0.6B completion still drifted into first person once during M3 verification -- flagged then, fixed now, not by tweaking that same instruction further.
  - **Layer 1**: `internal/hints.BuildHintPrompt` (prompt.go) now includes three worked Hint→Rephrase examples (`fewShotExamples`) drawn from real hint bank text, showing the model the shape of a correct second-person answer instead of only describing it -- the standard fix for small-model instruction drift, since describing a rule and demonstrating it land very differently on a 0.6B model.
  - **Layer 2**: `internal/hints.HasFirstPersonAuthorDrift` (validate.go, new) is a heuristic validator, not a second model call -- consistent with `Classify`'s existing style in this package (fixed, inspectable pattern matching over free text, not more inference). Flags "I"/"my" paired within a short span with a verb/noun tied to authoring code (forgot, wrote, opened, closed, my repeat/loop/block/...), deliberately narrow so it doesn't reject Pip's harmless first-person flavor text ("I bet you can do it!"). 10 unit test cases, including that exact distinction, all pass.
  - **Layer 3**: `internal/api.handleHint` now makes at most 2 completion attempts -- validate each, retry once on rejection, then fall back to the verified hint text verbatim (the same fallback path already used for a hard engine error) if the retry also fails validation. Two new integration tests (`TestIntegration_HintRetriesOnceAfterPerspectiveDrift`, `TestIntegration_HintFallsBackToVerifiedTextAfterTwoRejections`) exercise this through the real HTTP handler with a fake engine scripted to drift, confirming both the retry-then-accept and retry-then-fallback paths actually wire together correctly, not just that the validator function works in isolation.
  - **Acceptance check run for real, not simulated**: every hint in the bank (14 signature-specific hints across the 3 levels, plus `GenericFallback` -- 15 total) run through `BuildHintPrompt` and a real spawned `qwen3-0.6b-q4_k_m.gguf` 10 times each (150 real completions, `internal/hints/perspective_bench_manual_test.go`, `-tags=manual`). **Result: 0/150 rejected (0.0%)**, well under the 10% threshold the instruction set as the line between "prompt needs more work" and "validator is enough." The few-shot examples alone appear to have fully closed the gap that a bare instruction didn't, at least on this sample size -- the validator and retry/fallback stay in place regardless as the actual safety net, since 0% on 150 real samples doesn't prove 0% forever. Full per-hint breakdown logged in the test's own output; rerun anytime with `go test -tags=manual ./internal/hints/... -run TestManual_PerspectiveDriftRejectionRate -v`. Confirmed the spawned `llama-server.exe` process exited cleanly after the run (no orphan left behind).
- **2026-08-15 — Resolved the `unbalanced_block` client-trust question from QUESTIONS.md: stays client-asserted, but hardened from prose-matching to a structured code.** True server-side verification isn't feasible without a bigger protocol change: `compileAst.ts` silently truncates an unclosed block into a syntactically valid, complete AST before it's ever sent, so by the time `/api/program` sees it, the missing tail is indistinguishable from a program the child intentionally left short — the evidence only exists in the pre-compile workspace shape, which never crosses the wire. Given that, the client has to be the one to say so. What changed: `web/src/blocks/compileAst.ts`'s `CompileProblem` now carries a closed-enum `code` (`ProblemCode`: `nesting_too_deep` | `unclosed_block` | `orphan_closer`) alongside its human-readable `message`; `internal/hints.Classify` now matches on that exact code (`clientProblemUnclosedBlock`/`clientProblemOrphanCloser`) instead of `strings.Contains`-ing English prose fragments like `"never closed"`. The old approach had a real, silent-breakage risk: any future copy-edit to `compileAst.ts`'s message wording (translation, tone pass, anything) would have silently broken `unbalanced_block` detection with no test catching it, since nothing coupled the prose to the match. Accepting client-assertion at all is justified by bounded blast radius, documented directly on `ClassifyInput.ClientProblems` in `classify.go`: this signal only selects a key into a fixed, human-verified hint bank — it can never inject hint content, and it gates no score, save data, or unlock. A single-player offline kids' game has no adversarial party positioned to exploit it, and a kid faking it via devtools gains nothing but a mismatched (but still benign, pre-written) hint.
- **2026-08-15 — Resolved the `/api/program` body-shape limbo from QUESTIONS.md: dropped the legacy raw-AST shape entirely, don't keep both forever.** Checked for real callers before deciding: `web/src/api.ts` is the only production caller and has sent the `{"ast":...,"client_problems":[...]}` wrapper since the day M3 introduced it; the camera/Hub Mode pipeline isn't wired to call this endpoint yet at all (grepped `print/` and `scripts/` for any `POST /api/program` caller — none exists). Keeping dual-shape detection alive was protecting a hypothetical caller, not a real one — that's limbo, and when Hub Mode does eventually call this endpoint it should just be written against the current contract from day one rather than inheriting a legacy path. `handleProgram` now requires the wrapper shape strictly and returns 400 on anything else; `internal/api/api_test.go`'s three tests that previously posted raw-AST bodies were updated to the wrapper shape, and a new `TestIntegration_LegacyRawASTShapeIsRejected` locks in the 400 behavior so this doesn't silently regress back into dual-shape support later.
- **2026-08-15 — Model footprint audit: actual `models/` total is 1.9GB, not the ~6GB previously reported, and it's already exactly the 2 files the brief calls for.** `qwen3-0.6b-q4_k_m.gguf` is 484,220,320 bytes (~484MB/~0.45GiB) and `qwen3-1.7b-q5_k_m.gguf` is 1,471,805,856 bytes (~1.47GB/~1.37GiB) — sum ≈1.9GiB, matching the ~2GB expected for these two quants almost exactly. No extra quantizations, duplicate copies, or unused variants exist anywhere in the repo (checked `find . -iname "*.gguf"` from the repo root — only these two files exist anywhere under it). `bin/win/` (59MB, all DLLs + `launcher.exe` + `llama-server.exe`) and `bin/linux/` (15MB, launcher only — no ARM `llama-server` fetched yet, that's item 4) add negligibly on top. I can't reconcile the earlier "~6GB" figure against anything in this repo's actual state or against what's recorded in this file from when the GGUFs were originally fetched (see the entry above logging them at 484MB/~1.2GB at fetch time) — it doesn't match any real measurement I can find, on-disk now or logged previously. Treating the correct number as **~1.9GB total drive payload for models**, and flagging the discrepancy explicitly in `QUESTIONS.md` rather than silently reconciling it, since I can't explain where the higher figure came from.
